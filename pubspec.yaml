name: easyscan
description: Scan & Go retail app
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 📷 Camera + QR / Barcode scanner
  mobile_scanner: ^5.1.0

  # 🔊 Scan sound (beep)
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # 🔔 Assets for scan sound
  assets:
    - assets/sounds/beep.mp3
